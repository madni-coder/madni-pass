rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Images: only owner can read/write their own images
    match /images/{userId}/{allPaths=**} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
  }
}
